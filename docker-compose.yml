services:
  greenworks-booking:
    build: .
    container_name: greenworks-booking
    restart: unless-stopped
    env_file:
      - .env.local
    networks:
      - web

networks:
  web:
    external: true
